# 05 — Base de Données

> Hadara Suite v2.3.0 — Cartographie du modèle de données
> Date : 2026-08-26 | Marqueurs : 🟢🟡🟠🔴⚪🔵

---

## 1. Vue d'ensemble

```
┌─────────────────────────────────────────────────────────────────┐
│                    PostgreSQL (Render)                           │
│                                                                 │
│  ┌─────────────────────┐  ┌─────────────────────┐              │
│  │      api app         │  │    hadara_ai app     │              │
│  │                      │  │                      │              │
│  │  Brief               │  │  AIProvider          │              │
│  │  Client              │  │  AIProviderConfig    │              │
│  │  Template            │  │  PromptTemplate      │              │
│  │  PortfolioItem       │  │  PromptVersion       │              │
│  │  StoreProduct        │  │  AgentDefinition     │              │
│  │  BillingDocument     │  │  BriefAIAnalysis     │              │
│  │  BillingLine         │  │  AIExecution         │              │
│  │  Payment             │  │  ToolExecution       │              │
│  │  ToolUsageLog 🟠     │  │  UsageLog            │              │
│  │                      │  │  CostLog             │              │
│  │                      │  │  AIWorkflowExecution  │              │
│  │                      │  │  AIWorkflowStepExec   │              │
│  │                      │  │  AIAgentUsageLog      │              │
│  │                      │  │  AIDailyAggregate     │              │
│  └─────────────────────┘  └─────────────────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. Modèles Métier (api app)

### 2.1 Brief

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/api/models.py:22` |
| **Table** | `api_brief` |
| **PK** | `id` (CharField(50), auto `HAD-XXXX`) |
| **Ordering** | `-created_at` |
| **Verbose** | "Brief & Projet" |

**Champs** :

| Champ | Type | Contraintes | Notes |
|-------|------|------------|-------|
| `id` | CharField(50) | PK, auto (`HAD-XXXX`) | Identifiant unique |
| `status` | CharField(50) | default=`nouveau` | `Enum:BriefStatus` |
| `project_type` | CharField(50) | nullable | `Enum:ProjectType` |
| `project_type_custom` | CharField(200) | nullable | Si project_type=autre |
| `technical_format` | CharField(50) | nullable | `Enum:TechnicalFormat` |
| `custom_dimensions` | CharField(200) | nullable | Si technical_format=custom |
| `usage_type` | CharField(20) | nullable | `Enum:UsageType` |
| `budget_range` | CharField(50) | nullable | `Enum:BudgetRange` |
| `preferred_colors` | CharField(200) | nullable | Couleurs préférées |
| `avoid_colors` | CharField(200) | nullable | Couleurs à éviter |
| `style_preferences` | JSONField | default=list | `Enum:StylePreference[]` |
| `context_description` | TextField | nullable | Contexte & Description |
| `primary_objective` | TextField | nullable | Objectif Principal |
| `target_audience` | TextField | nullable | Cible Visée |
| `target_audience_chips` | JSONField | default=list | Tags Cible |
| `full_text_content` | TextField | nullable | Textes Inclus |
| `main_title` | CharField(200) | nullable | Titre Principal |
| `attachments` | JSONField | default=list | Pièces Jointes |
| `deliverable_versions` | JSONField | default=list | Versions de Livrables (V1, V2, V3) |
| `client_name` | CharField(200) | nullable | Client (snapshot) |
| `organization` | CharField(200) | nullable | Organisation (snapshot) |
| `email` | EmailField | nullable | Email (snapshot) |
| `whatsapp` | CharField(50) | nullable | WhatsApp (snapshot) |
| `city_country` | CharField(200) | nullable | Ville / Pays |
| `critical_deadline` | CharField(50) | nullable | Délai Critique |
| `desired_delivery_date` | CharField(50) | nullable | Date de Livraison Souhaitée |
| `reference_links` | TextField | nullable | Liens de Référence |
| `quoted_price_fcfa` | IntegerField | nullable | Devis Estimé (FCFA) |
| `designer_notes` | TextField | nullable | Notes du Designer |
| `ai_analysis` | JSONField | nullable | Analyse IA Hadara |
| `accept_process` | BooleanField | default=False | Processus Validé |
| `accept_deadlines` | BooleanField | default=False | Délais Validés |
| `client` | FK→Client | nullable, SET_NULL | Client (Facturation) |
| `created_at` | DateTimeField | auto_now_add | Date de Création |

**Relations** :
- `client` → `Model:Client` (nullable FK)
- `billing_documents` → `Model:BillingDocument` (reverse)
- `briefaianalysis_set` → `Model:BriefAIAnalysis` (reverse)

---

### 2.2 Client

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/api/models.py:6` |
| **Table** | `api_client` |
| **PK** | `id` (CharField(50), auto) |

**Champs** :

| Champ | Type | Contraintes |
|-------|------|------------|
| `name` | CharField(200) | required |
| `organization` | CharField(200) | nullable |
| `whatsapp` | CharField(50) | nullable |
| `email` | EmailField | nullable |
| `address` | TextField | nullable |
| `created_at` | DateTimeField | auto_now_add |

---

### 2.3 Template

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/api/models.py:95` |
| **Table** | `api_template` |
| **PK** | `id` (CharField(50), auto `TPL-XXXX`) |

**Champs** :

| Champ | Type | Contraintes | Notes |
|-------|------|------------|-------|
| `id` | CharField(50) | PK, auto (`TPL-XXXX`) | Identifiant unique |
| `title` | CharField(200) | required | Titre du modèle |
| `category` | CharField(100) | required | Catégorie |
| `project_type` | CharField(50) | required | Type de projet |
| `technical_format` | CharField(50) | required | Format technique |
| `description` | TextField | nullable | Description |
| `custom_dimensions` | CharField(200) | nullable | Dimensions |
| `default_main_title` | CharField(200) | nullable | Titre par défaut |
| `default_full_text_content` | TextField | nullable | Contenu texte par défaut |
| `style_preferences` | JSONField | default=list | Préférences de style |
| `preferred_colors` | CharField(200) | nullable | Couleurs souhaitées |
| `avoid_colors` | CharField(200) | nullable | Couleurs à éviter |
| `default_budget_range` | CharField(50) | nullable | Fourchette de budget |
| `suggested_price_fcfa` | IntegerField | nullable | Prix Indicatif (FCFA) |
| `usage_count` | IntegerField | default=0 | Nombre d'utilisations |

---

### 2.4 PortfolioItem

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/api/models.py:138` |
| **Table** | `api_portfolioitem` |
| **PK** | `id` (CharField(50), auto `PRT-XXXX`) |

**Champs** :

| Champ | Type | Contraintes | Notes |
|-------|------|------------|-------|
| `id` | CharField(50) | PK, auto (`PRT-XXXX`) | Identifiant unique |
| `created_at` | DateTimeField | auto_now_add | Date de création |
| `title` | CharField(200) | required | Titre du projet |
| `category` | CharField(100) | required | Catégorie |
| `image_url` | TextField | nullable | Image du projet (URL ou Upload) |
| `description` | TextField | nullable | Description |
| `badge` | CharField(100) | nullable | Badge Visuel |
| `price_estimate` | CharField(100) | nullable | Tarif Indicatif |
| `accent_hex` | CharField(50) | default=`#816C07` | Couleur d'Accent Visuel |
| `features` | JSONField | default=list | Livrables Inclus |

---

### 2.5 StoreProduct

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/api/models.py:175` |
| **Table** | `api_storeproduct` |
| **PK** | `id` (CharField(50), auto `PRD-XXXX`) |

**Champs** :

| Champ | Type | Contraintes | Notes |
|-------|------|------------|-------|
| `id` | CharField(50) | PK, auto (`PRD-XXXX`) | Identifiant unique |
| `name` | CharField(200) | required | Nom du produit |
| `brand` | CharField(120) | nullable | Marque |
| `category` | CharField(100) | required | Catégorie |
| `description` | TextField | nullable | Description |
| `image` | TextField | nullable | Image (URL ou Fichier) |
| `status` | CharField(20) | default=`on_order` | `Enum:StoreProductStatus` |
| `featured` | BooleanField | default=False | En Vedette |
| `visible` | BooleanField | default=True | Visible en Boutique |
| `price` | PositiveIntegerField | nullable | Prix (FCFA) |
| `created_at` | DateTimeField | auto_now_add | Date de création |
| `updated_at` | DateTimeField | auto_now | Date de mise à jour |

---

### 2.6 BillingDocument

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/api/models.py:115` |
| **Table** | `api_billingdocument` |
| **PK** | `id` (BigAutoField) |

**Champs** :

| Champ | Type | Notes |
|-------|------|-------|
| `document_number` | CharField(30) | unique, auto |
| `doc_type` | CharField(15) | `Enum:DocType` |
| `payment_status` | CharField(20) | `Enum:PaymentStatus` |
| `billing_client_name` | CharField(200) | snapshot |
| `billing_organization` | CharField(200) | snapshot |
| `billing_address` | TextField | snapshot |
| `billing_email` | CharField(200) | snapshot |
| `billing_whatsapp` | CharField(50) | snapshot |
| `subtotal` | PositiveIntegerField | FCFA |
| `discount` | PositiveIntegerField | FCFA |
| `total` | PositiveIntegerField | FCFA |
| `currency` | CharField(5) | default=XOF |
| `issue_date` | DateField | auto_now_add |
| `due_date` | DateField | nullable |
| `notes` | TextField | nullable |
| `brief` | FK→Brief | nullable, SET_NULL |
| `client` | FK→Client | nullable, SET_NULL |

**Relations** :
- `lines` → `Model:BillingLine` (reverse)
- `payments` → `Model:Payment` (reverse)

---

### 2.7 BillingLine

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/api/models.py:150` |
| **Table** | `api_billingline` |

**Champs** : `id`, `designation`, `quantity` (Decimal), `unit_price` (FCFA), `document` (FK→BillingDocument, CASCADE)

---

### 2.8 Payment

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/api/models.py:165` |
| **Table** | `api_payment` |
| **PK** | `id` (CharField(50), auto) |

**Champs** :

| Champ | Type | Notes |
|-------|------|-------|
| `amount` | PositiveIntegerField | FCFA |
| `method` | CharField(20) | `Enum:PaymentMethod` |
| `reference_code` | CharField(200) | ex: WAVE-XXXXXX |
| `payment_date` | DateField | required |
| `note` | CharField(300) | ex: Acompte |
| `billing_document` | FK→BillingDocument | CASCADE |

---

### 2.9 ToolUsageLog (🟠 Legacy)

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/api/models.py:187` |
| **Table** | `api_toolusagelog` |
| **État** | 🟠 (remplacé par `ToolExecution`) |

---

## 3. Modèles IA (hadara_ai app)

### 3.1 AIProvider

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/provider.py` |
| **Table** | `hadara_ai_aiprovider` |

**Champs** : `id`, `name`, `provider_type`, `is_default`

### 3.2 AIProviderConfig

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/provider.py` |
| **Table** | `hadara_ai_aiproviderconfig` |

**Champs** : `id`, `provider` (FK→AIProvider), `config_key`, `config_value`, `is_secret`

### 3.3 PromptTemplate

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/prompt.py` |
| **Table** | `hadara_ai_prompttemplate` |

**Champs** : `id`, `name`, `template_type`, `agent_type`, `content`, `is_active`

### 3.4 PromptVersion

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/prompt.py` |
| **Table** | `hadara_ai_promptversion` |

**Champs** : `id`, `template` (FK→PromptTemplate), `version`, `content`, `created_at`

### 3.5 AgentDefinition

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/agent.py` |
| **Table** | `hadara_ai_agentdefinition` |

**Champs** : `id`, `name`, `slug`, `description`, `model_name`, `temperature`, `max_tokens`, `tools`, `is_active`

### 3.6 BriefAIAnalysis

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/analysis.py` |
| **Table** | `hadara_ai_briefaianalysis` |

**Champs** : `id`, `brief` (FK→Brief), `agent_type`, `analysis` (JSON), `model_used`, `confidence_score`, `created_at`

### 3.7 AIExecution

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/trace.py` |
| **Table** | `hadara_ai_aiexecution` |

**Champs** : `id`, `agent_type`, `execution_id` (UUID), `model_name`, `status` (`Enum:ExecutionStatus`), `input_messages` (JSON), `output_response` (text), `tokens_input`, `tokens_output`, `latency_ms`, `estimated_cost_usd`, `provider_id`, `metadata` (JSON), `created_at`

### 3.8 ToolExecution

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/trace.py` |
| **Table** | `hadara_ai_tookexecution` |

**Champs** : `id`, `execution` (FK→AIExecution), `tool_name`, `input_data` (JSON), `output_data` (JSON), `success`, `latency_ms`, `error_message`, `created_at`

### 3.9 UsageLog

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/trace.py` |
| **Table** | `hadara_ai_usagelog` |

**Champs** : `id`, `date`, `provider`, `model`, `agent_type`, `tool_name`, `total_calls`, `total_tokens_in`, `total_tokens_out`, `total_cost_usd`, `avg_latency_ms`, `error_count`

### 3.10 CostLog

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/trace.py` |
| **Table** | `hadara_ai_costlog` |

**Champs** : `id`, `execution` (FK→AIExecution), `provider`, `model`, `tokens_in`, `tokens_out`, `estimated_cost_usd`, `created_at`

### 3.11 AIWorkflowExecution

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/workflow.py` |
| **Table** | `hadara_ai_aiworkflowexecution` |

**Champs** : `id`, `workflow_id` (UUID), `brief` (FK→Brief), `status`, `started_at`, `completed_at`

### 3.12 AIWorkflowStepExecution

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/workflow.py` |
| **Table** | `hadara_ai_aiworkflowstepexecution` |

**Champs** : `id`, `workflow` (FK→AIWorkflowExecution), `agent_type`, `status`, `execution` (FK→AIExecution), `started_at`, `completed_at`, `order`

### 3.13 AIAgentUsageLog

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/analytics.py` |
| **Table** | `hadara_ai_aiagentusagelog` |

**Champs** : `id`, `date`, `agent_type`, `total_runs`, `avg_latency_ms`, `total_tokens`, `total_cost_usd`, `success_count`, `error_count`

### 3.14 AIDailyAggregate

| Propriété | Valeur |
|-----------|--------|
| **Emplacement** | `backend/hadara_ai/models/analytics.py` |
| **Table** | `hadara_ai_aidailyaggregate` |

**Champs** : `id`, `date`, `total_runs`, `total_tokens`, `total_cost_usd`, `avg_latency_ms`, `agent_breakdown` (JSON), `model_breakdown` (JSON), `error_rate`

---

## 4. Relations

```
Client ──┬──< Brief ──┬──< BillingDocument ──< BillingLine
         │            │                    └──< Payment
         │            │
         │            ├──< BriefAIAnalysis
         │            │
         │            ├──< AIWorkflowExecution ──< AIWorkflowStepExecution
         │            │
         │            └──< ToolUsageLog 🟠
         │
         └──< BillingDocument

AIProvider ──< AIProviderConfig

PromptTemplate ──< PromptVersion

AIExecution ──< ToolExecution
            ──< CostLog
            ──< AIWorkflowStepExecution
```

---

## 5. Enums / Statuts

| Enum | Valeurs | Modèle |
|------|---------|--------|
| `BriefStatus` | nouveau, devis_envoye, acompte_recu, en_creation, validation, termine | Brief |
| `ProjectType` | affiche, bache, flyer, identite_visuelle, site_web, reseaux_sociaux, pack_starter, pack_event, pack_hajj_oumrah, broderie, autre | Brief |
| `TechnicalFormat` | A3_A4, bache_3x1, flyer_A5, post_RS, story_vertical, sur_mesure | Brief |
| `BudgetRange` | 30k-50k, 50k-80k, 80k-120k, sur_devis | Brief |
| `StylePreference` | moderne, classique, minimaliste, luxueux, spirituel, traditionnel | Brief |
| `StoreProductStatus` | in_stock, available_24_48h, on_order, unavailable | StoreProduct |
| `DocType` | proforma, facture, avoir | BillingDocument |
| `PaymentStatus` | brouillon, en_attente, acompte, partiel, paye, en_retard, annule | BillingDocument |
| `PaymentMethod` | wave, orange_money, especes, virement, cheque, autre | Payment |
| `ExecutionStatus` | pending, running, completed, failed, timeout, rate_limited, cancelled | AIExecution |

---

## 6. Stratégie de Rétention

**Emplacement** : `RETENTION_POLICY` dans `backend/hadara_ai/models/trace.py`

| Type | Rétention |
|------|-----------|
| Défaut | 365 jours |
| Échoué | 90 jours |
| Annulé | 30 jours |
